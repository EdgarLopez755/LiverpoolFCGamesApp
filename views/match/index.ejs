
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Matches</title>

</head>
<body>
  
    <h1>All Matches</h1>
    <form action="/matches" method="POST"></form>
    <ul>
        <% matches.forEach(match => { %>
            <li>
                <a href="/matches/<%= matches._id %>">
                    <%= match.homeTeam.name %> vs. <%= match.awayTeam.name %>
                </a>
                <span> (<%= match.homeScore %> - <%= match.awayScore %>) </span>
                <span> - <%= new Date(match.date).toLocaleDateString() %></span>
                <a href="/matches/<%= match._id %>/edit">Edit</a>
                <a href="/matches/<%= match._id %>/delete" onclick="return confirm('Are you sure you want to delete this match?')">Delete</a>
            </li>
        <% }); %>
    </ul>
</body>
</html>